<div class= 'row'>
<div class="col s12">
<h3 class= 'align center'>Course Listing</h3>
<p class= 'align center'>Tech university is about changing the world of technology. Some of our introductory course are listed below. Other courses can be accessed by clicking on the courses link</h3>
</div>
</div>
<div class="row">
<% @courses.each do |course| %>
    <div class="col s4  align center">
      <div class="card hoverable">
        <div class="card-image">
         <%= image_tag("computer") %> 
          <span class="card-title yellow accent-3"><%= course.symbol %></span>
        </div>
        <div class="card-content">
          <p><%= course.name %></p>
          <p><%= course.description %></p>
        </div>
        <div class="card-action yellow accent-3">
        <% if logged_in? && !(current_user.courses.include?(course)) %>
          <%=link_to 'Enroll', course_enroll_path(course_id: course.id), method: :post %>
          <% end %>
          <%=link_to 'Info', course_path(course) %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
